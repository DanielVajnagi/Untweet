<div class="container mx-auto my-8">
  <div class="bg-white p-6 rounded shadow">
    <h1 class="text-3xl font-bold">Welcome, <%= current_user.username %>!</h1>
    <p class="mt-4">You have successfully logged in.</p>
    
    <div class="mt-6">
      <h2 class="text-xl font-semibold"></h2>
      <p class="mt-2">This is placeholder page, it will be changed soon</p>
    </div>

    <div class="mt-8">
      <%= link_to 'Log Out', destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-danger bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded' %>
    </div>
  </div>
</div>
