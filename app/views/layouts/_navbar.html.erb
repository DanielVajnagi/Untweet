<nav class="navbar">
  <div class="container">
    <div class="title">
      <%= link_to 'Home', root_path %>
    </div>
    <div>
      <% if user_signed_in? %>
        <span class="text-white">Welcome, <%= current_user.username %>!</span>
        <%= link_to 'Edit Profile', edit_user_registration_path, class: 'link' %>
        <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'link' %>
      <% else %>
        <%= link_to 'Login', new_user_session_path, class: 'link' %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'link' %>
      <% end %>
    </div>
  </div>
</nav>
